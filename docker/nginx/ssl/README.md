# SSL certificates should be placed here

# For production, use Let's Encrypt or your SSL provider

# Example structure:

# - fullchain.pem (or cert.pem)

# - privkey.pem (or key.pem)

# For Let's Encrypt with Certbot, you can mount the live certificates:

# volumes:

# - /etc/letsencrypt/live/yourdomain.com/fullchain.pem:/etc/nginx/ssl/fullchain.pem:ro

# - /etc/letsencrypt/live/yourdomain.com/privkey.pem:/etc/nginx/ssl/privkey.pem:ro
